# Feature 12 Welcome To Nurt Plan

**Last Updated:** 2026-04-07 03:03:34 PM
**Status:** Completed
**Most Recent Session Summary:** `docs/session-summaries/SESSION_143_SUMMARY.md`

## Goal

Implement feature `12.0` as a bare-minimum but clearly branded `Welcome To Nurt` demo across `web`, `desktop`, `mobile`, and `tv`, replacing the old placeholder copy with a balanced starter-guide experience that explains the generated repo baseline without breaking the shared-boundary rules from feature `11.0`.

## Completion Summary

- [x] Updated the frontend scaffold contracts so the feature `12.0` baseline is locked semantically across `web`, `desktop`, `mobile`, and `tv`.
- [x] Updated `packages/shared` welcome exports so the baseline now includes shared highlights, starter-guidance steps, and TV instructional-card content.
- [x] Updated the web starter route to render the nurt-branded starter-guide baseline with shared highlights and starter-guidance sections while keeping route ownership in the web app.
- [x] Updated the desktop renderer baseline to adopt the same welcome concept with desktop-local rendering and routing.
- [x] Updated the mobile starter app to present the same concept in a touch-friendly stacked layout.
- [x] Updated the TV starter app to use focusable instructional cards (`Start Building`, `How This Repo Works`, `Shared Foundations`) plus a detail panel instead of generic placeholder rail items.
- [x] Refreshed bundled snapshot metadata with `uv run python -m new_repo_template.nurt_cli template-assets validate --source-root "."`.
- [x] Revalidated the feature with targeted frontend contracts (22 passed), `uv run ruff check src/new_repo_template tests/contracts`, and `uv run pytest` (248 passed).

## Locked Outcome

- [x] The `Welcome To Nurt` baseline is now cross-frontend rather than web-only.
- [x] The starter remains a balanced starter-guide rather than a long tutorial or generic framework placeholder.
- [x] The content stays mostly conceptual rather than command-heavy.
- [x] Shared content lives in `@generated/shared`, while rendering and interaction stay target-local.
- [x] TV keeps its remote-primary/focus-first interaction model instead of being forced into generalized shared navigation behavior.
- [x] Feature `13.0` still owns the fuller component-system follow-through.

## Next Likely Target

- [ ] Discuss and lock feature `13.0` before building the component-strategy refinement pass on top of the now-real `Welcome To Nurt` baseline.
- [ ] Read `PLAN.md`, `PROGRESS.md`, `docs/LIVING_DOCS.md`, `docs/ARCHITECTURE.md`, `TODO-FEATURES.md`, and `docs/session-summaries/SESSION_143_SUMMARY.md` before starting the next planning slice.
- [ ] Run `btca status` and use plain `btca ask` lookups for any feature `13.0` library/component-strategy questions before editing.
