---
description: "Setup or Update btca config for current project"
---

Set up `btca` for this project: scan package.json and/or pyproject.toml for major dependencies (frameworks, libraries, tools), suggest adding each as a btca resource with sensible defaults, then create a btca.config.jsonc file in the project root and update AGENTS.md with usage instructions. If the btca.config.jsonc file already exists, update both it and the AGENTS.md file to keep everything current and in-sync. **IMPORTANT**: Ask me to confirm each resource before adding.
