---
description: "Make a PR, merge it, fix any CI errors, and clean up the branch."
---

IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

**IMPORTANT**: Use the `gh` command tool to make, update, watch and merge a PR.

<steps CRITICAL="TRUE">
1. Push this branch, then open a PR.
2. The PR is for this ENTIRE branch. Review the history and work done, then create an appropriate title and thoroughly detailed body for the PR.
3. Push the PR and watch it. Analyze, investigate and fix or resolve any CI errors. The PR should be passing all Green checks.
4. Finally, merge the branch/PR. For PRs smaller in scope, use `rebase`; for larger PRs, use `squash`.
5. If everything is successful, cleanup and remove the branch from remote and local when done.
6. Give a detailed report when complete.
</steps>
