{
  "name": "@generated/backend",
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "convex": "^1.32.0"
  },
  "scripts": {
    "dev": "bun run convex:dev:smoke",
    "build": "bun --version",
    "test": "bun run convex:codegen:smoke",
    "lint": "bun --version",
    "typecheck": "bun --version",
    "convex:codegen": "convex codegen",
    "convex:dev": "convex dev",
    "convex:codegen:smoke": "convex codegen --help",
    "convex:dev:smoke": "convex dev --help"
  }
}
