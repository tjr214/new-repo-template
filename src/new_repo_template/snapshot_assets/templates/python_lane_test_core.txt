from python_app.core import build_starter_summary


def test_build_starter_summary_includes_requested_subject() -> None:
    summary = build_starter_summary("demo-user")

    assert "demo-user" in summary.greeting
    assert summary.commands[0] == "uv run python-app --help"
